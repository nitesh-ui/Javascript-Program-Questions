
let name = "kkaran";
let name1 = name.split("");
for(let i = 0 ; i < name1.length ; i++){
    if(name1.indexOf(name[i])===name1.lastIndexOf(name[i])){
        console.log(name[i]);
        break;
    }
}
