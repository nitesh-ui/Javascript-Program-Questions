let arr = [1, 2, 3, 4];

//Method 1
arr.reverse();
console.log(arr); //[4, 3, 2, 1]

//Method 2
let newArr = [];
for(let i = arr.length-1; i >= 0; i--) {
  newArr.push(arr[i]);
}
console.log(newArr);
