let arr = [ 1, 2, 3, 4, 5 ];

let minI = arr[0];
let maxI = arr[0];

for(let i = 0; i < arr.length; i++) {
  if(arr[i] < minI) {
    minI = arr[i];
  }
  else if(arr[i] > maxI) {
    maxI = arr[i];
  }
}
console.log(minI); // 1
console.log(maxI); //5
