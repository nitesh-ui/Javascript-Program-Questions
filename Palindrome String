isPalindrome(S){

        for (var i = 0; i < S.length / 2; i++) {
          if (S[i] !== S[S.length - i - 1]) {
            return "S is not palindrome";
          }
        }
        return "S is palindrome";
    }
